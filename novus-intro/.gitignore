# Generated by `npx hyperframes check --snapshots` / `snapshot` — rebuilt on demand.
snapshots/
contact-sheet.jpg

# Render outputs. Re-render with `npm run render`.
renders/

# Scratch space the CLI uses during renders.
.hf-tmp/

node_modules/
