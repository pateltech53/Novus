# Phase 1 prompt — superseded

Phase 1 now lives in **[`BUILD-PROMPT.md`](./BUILD-PROMPT.md)**, which carries all remaining phases
(1–7) in one document, each with its own gate and stop rule.

Nothing was lost — the Phase 1 content, the measured before-scoreboard, the file-level task list,
and the gate all moved there verbatim. This stub exists so older links keep resolving.

**Reading order for a fresh session:**

1. `design.md` — the locked design system
2. `docs/DO-NOT-TOUCH.md` — protected files + Brand Laws
3. `docs/BASELINE.md` — the measured pre-Phase-1 state
4. `docs/BUILD-PROMPT.md` — the work
