# Copy to .env.local and fill in. .env.local is already gitignored.
# Values come from your Supabase project: Settings → API.

# ── Supabase ────────────────────────────────────────────────────────────────
# Public. Safe in a bundle — RLS is what protects the data, not this key.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SERVER ONLY. Bypasses RLS entirely. Never prefix with NEXT_PUBLIC_, and never
# import into a file a Client Component can reach — one "use client" at the top
# of the wrong file publishes this to every browser.
SUPABASE_SERVICE_ROLE_KEY=

# ── Leaderboard ─────────────────────────────────────────────────────────────
# Pins replay compatibility. Bump when the engine or the event library changes,
# because old tapes stop replaying to the same numbers when content moves.
NOVUS_ENGINE_VERSION=1
NOVUS_LEADERBOARD_SEASON=2026-Q3

# ── AI endpoints (already used by lib/ai/*) ─────────────────────────────────
NEXT_PUBLIC_TTS_ENDPOINT=
NEXT_PUBLIC_STT_ENDPOINT=
NEXT_PUBLIC_PITCH_ENDPOINT=
